import styles from './Footer.module.css';

export default function Footer() {

    return (
        <section>
            <h1 className={styles.heading}>This is the footer</h1>
        </section>
    )

}